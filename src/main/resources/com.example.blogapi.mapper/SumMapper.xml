<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blogapi.mapper.SumMapper">
    <select id="getSum" resultType="com.example.blogapi.pojo.SumEntity">
        SELECT SUM(browse) as visitTotal, SUM(article.id) as articleTotal, SUM(comment.id) as commentTotal
        FROM springbootmanager.article as article,
             springbootmanager.comment
    </select>
</mapper>